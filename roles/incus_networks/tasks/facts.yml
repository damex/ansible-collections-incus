---
- name: Ensure incus networks cluster facts
  ansible.builtin.command:
    cmd: incus cluster show {{ inventory_hostname }}
  register: incus_networks_cluster_info_state
  failed_when: false
  changed_when: false
  become: true
