---
- name: Ensure incus projects
  tags:
    - incus
    - incus_projects
  block:
    - name: Ensure incus projects facts
      ansible.builtin.import_tasks: facts.yml
    - name: Ensure incus projects
      ansible.builtin.import_tasks: projects.yml
